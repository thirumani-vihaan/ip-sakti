# Optional dependencies for LIVE mode (real Gemini / Bhashini calls).
# The offline core (requirements.lock.txt) intentionally excludes these heavy SDKs.
# Install only when going live:  pip install -r requirements-real.txt
-r requirements.lock.txt
google-genai==2.20.0
